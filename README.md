# News-radar
A Python automation bot that scrapes RSS feeds, filters articles by keyword using regex, stores results in SQLite with deduplication, and sends a styled HTML email digest daily via APScheduler — with CLI and 25+ pytest tests.
